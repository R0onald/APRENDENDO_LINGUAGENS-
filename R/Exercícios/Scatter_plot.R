# Scatter plot
plot_dispersao <- function(x, y) {
  plot(x, y, main = "Dispersão entre variável X e variável Y", xlab = "X", ylab = "Y")
}

# Exemplo de uso:
# plot_dispersao(dados$variavel_x, dados$variavel_y)
